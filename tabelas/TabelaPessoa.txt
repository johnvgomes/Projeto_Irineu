-- create database processo seletivo;


-- criando FN3

-- criando tabela pessoa


create table pessoa(
	
	cdPessoa auto_increment int ,
	CPF int unique NOT NULL,
	nome_Completo varchar(200),	
	data_Nasc date NOT NULL,
	RG int  NOT NULL,
	emissor_RG varchar(200)
	sexo varchar(200) NOT NULL,
	passaporte varchar(200) NOT NULL,
	salario real NOT NULL,
	cd_endereco int NOT NULL,
	
	primary key (cdPessoa),

	foreign key (cd_endereco) references endereco(cdEndereco)
);
